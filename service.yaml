apiVersion: v1
kind: Service
metadata:
  name: emqx
  namespace: emqx
spec:
  clusterIP: None     # HEADLESS — required for pod DNS
  selector:
    app: emqx
  ports:
  - name: mqtt
    port: 1883
  - name: dashboard
    port: 18083
